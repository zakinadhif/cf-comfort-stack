{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --persist-to=../../.cloudflare-state",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@myapp/auth": "workspace:^",
    "@myapp/db": "workspace:^",
    "@myapp/api-zod": "workspace:^",
    "drizzle-orm": "catalog:",
    "hono": "^4.12.8"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "vitest": "^3.2.4",
    "wrangler": "^4.74.0"
  }
}
